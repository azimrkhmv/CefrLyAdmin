# Same Supabase project as the student app. The anon key is safe in the browser:
# every table is RLS'd and every admin action goes through an edge function
# that re-checks profiles.role server-side.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>

# Where the "Student site" link in the header points.
VITE_STUDENT_URL=https://cefrly.vercel.app
